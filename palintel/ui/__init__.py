"""Local console for managing and inspecting the bot.

    python -m palintel.ui

Loopback only, token-gated. Reads the same save the bot reads and the same session files
the bot writes; see `sources` for why it deliberately does not depend on the bot process.
"""
